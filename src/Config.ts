import { GameConfig } from './features/apple-catch/types';

export const GAME_CONFIG: GameConfig = {
  basketSpeed: 10,
  appleSpeed: 2,
  gameWidth: 400,
  gameHeight: 600,
  basketWidth: 60,
  basketHeight: 40,
  appleSize: 25,
  initialLives: 3,
  normalApplePoints: 1,
  bonusApplePoints: 10,
  appleSpawnRate: 0.02,
  bonusAppleSpawnRate: 0.005,
  bonusAppleInterval: 600,
};